export default function (ctx) {
    return {
        baseSkin: {
            cardWidth: 100,
            cardHeight: 100,
            minValueVisibleHeight: 40,
            minValueVisibleWidth: 40,
            ctx,
        }
    };
}
